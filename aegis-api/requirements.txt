:root {
  font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  color: #e2e8f0;
  background: #020817;
  line-height: 1.5;
  font-weight: 400;
  font-synthesis: none;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

* {
  box-sizing: border-box;
}

html, body, #root {
  margin: 0;
  min-height: 100%;
  height: 100%;
  background: #020817;
}

body {
  min-height: 100vh;
}

button, input {
  font: inherit;
}

.app-shell {
  display: flex;
  min-height: 100vh;
  background: radial-gradient(circle at top, #0f172a 0%, #020817 52%);
}

.sidebar {
  width: 280px;
  border-right: 1px solid rgba(148, 163, 184, 0.25);
  background: rgba(15, 23, 42, 0.9);
  padding: 24px 18px;
}

.brand-block {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 28px;
}

.brand-mark {
  display: grid;
  place-items: center;
  width: 42px;
  height: 42px;
  border-radius: 12px;
  background: linear-gradient(135deg, #38bdf8, #8b5cf6);
  color: white;
  font-weight: 700;
}

.brand-block h1 {
  margin: 0;
  font-size: 1.3rem;
}

.brand-block p {
  margin: 0;
  color: #94a3b8;
  font-size: 0.80rem;
}

.nav-stack {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.nav-item {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 10px 12px;
  background: transparent;
  color: #dbeafe;
  border: 1px solid transparent;
  border-radius: 10px;
  cursor: pointer;
  text-align: left;
}

.nav-item.active {
  background: rgba(59, 130, 246, 0.18);
  border-color: rgba(96, 165, 250, 0.4);
}

.status-panel {
  margin-top: 30px;
  padding: 14px 12px;
  border-radius: 12px;
  background: rgba(15, 118, 110, 0.12);
  border: 1px solid rgba(45, 212, 191, 0.2);
}

.status-panel h3 {
  margin: 0 0 12px 0;
  font-size: 0.95rem;
}

.status-panel ul {
  list-style: none;
  padding: 0;
  margin: 0;
  display: grid;
  gap: 6px;
  color: #cbd5e1;
  font-size: 0.8rem;
}

.content-panel {
  flex: 1;
  padding: 24px;
}

.topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
}

.eyebrow {
  margin: 0 0 6px;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: #7dd3fc;
  font-size: 0.72rem;
}

.topbar h2 {
  margin: 0;
  font-size: 2rem;
}

.header-badges {
  display: flex;
  gap: 10px;
}

.badge {
  display: inline-flex;
  border-radius: 999px;
  padding: 7px 10px;
  font-size: 0.78rem;
}

.badge.success {
  background: rgba(52, 211, 153, 0.16);
  color: #bbf7d0;
}

.badge.neutral {
  background: rgba(148, 163, 184, 0.16);
  color: #e2e8f0;
}

.hero-grid {
  display: grid;
  grid-template-columns: 2fr 1fr;
  gap: 18px;
  margin-bottom: 18px;
}

.viewport-card,
.sld-card,
.card,
.panel {
  background: rgba(15, 23, 42, 0.8);
  border: 1px solid rgba(148, 163, 184, 0.2);
  border-radius: 16px;
  overflow: hidden;
}

.canvas-frame {
  height: 420px;
  position: relative;
}

.viewport-controls {
  position: absolute;
  top: 12px;
  left: 12px;
  z-index: 3;
  display: flex;
  gap: 10px;
  background: rgba(15, 23, 42, 0.82);
  border: 1px solid rgba(148, 163, 184, 0.2);
  border-radius: 10px;
  padding: 8px 10px;
}

.viewport-controls label {
  font-size: 0.72rem;
  color: #dbeafe;
  display: inline-flex;
  gap: 6px;
  align-items: center;
}

.sld-wrap {
  min-height: 420px;
  padding: 8px;
}

.tab-content {
  min-height: 300px;
}

.tab-grid {
  display: grid;
  gap: 18px;
}

.tab-grid.two-col {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.card {
  padding: 18px;
}

.card h3 {
  margin: 0 0 16px;
  font-size: 1rem;
}

.metric-list {
  display: grid;
  gap: 12px;
}

.metric-row {
  display: grid;
  grid-template-columns: 1.5fr auto auto;
  gap: 8px;
  align-items: center;
  color: #e2e8f0;
}

.metric-row strong {
  font-size: 1rem;
}

.metric-row .blue { color: #7dd3fc; }
.metric-row .green { color: #6ee7b7; }
.metric-row .amber { color: #fbbf24; }

.stack-chart {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.bar {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 42px;
  color: #f8fafc;
  border-radius: 10px;
  font-size: 0.8rem;
}

.bar.blue { background: rgba(59, 130, 246, 0.7); }
.bar.green { background: rgba(16, 185, 129, 0.7); }
.bar.amber { background: rgba(245, 158, 11, 0.7); }
.bar.orange { background: rgba(249, 115, 22, 0.7); }

.line-plot {
  position: relative;
  height: 180px;
  border-radius: 12px;
  background: linear-gradient(180deg, rgba(59,130,246,0.08), rgba(15,23,42,0.2));
  border: 1px solid rgba(148,163,184,0.2);
}

.line-point {
  position: absolute;
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: #7dd3fc;
  transform: translate(-50%, -50%);
}

.gauge {
  position: relative;
  width: 100%;
  height: 40px;
  border-radius: 999px;
  background: rgba(148,163,184,0.15);
  overflow: hidden;
  border: 1px solid rgba(148,163,184,0.2);
}

.gauge-fill {
  position: absolute;
  inset: 0 auto 0 0;
  background: linear-gradient(90deg, #22c55e, #fbbf24, #f97316);
}

.gauge span {
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;
  font-weight: 700;
}

.table-mini {
  width: 100%;
  border-collapse: collapse;
}

.table-mini td, .table-mini th {
  padding: 8px 6px;
  border-bottom: 1px solid rgba(148,163,184,0.15);
  text-align: left;
}

.wide {
  width: 100%;
}

.curve-svg {
  width: 100%;
  height: 180px;
  background: rgba(15,23,42,0.4);
  border-radius: 12px;
}

.bar-chart {
  display: flex;
  align-items: end;
  gap: 10px;
  height: 200px;
  padding-top: 20px;
}

.bar-stack {
  flex: 1;
  display: flex;
  align-items: end;
  justify-content: center;
  height: 100%;
}

.bar-stack span {
  width: 100%;
  border-radius: 10px 10px 0 0;
  background: linear-gradient(180deg, #60a5fa, #2563eb);
  min-height: 20px;
}

.controls-row {
  display: grid;
  gap: 8px;
  margin-bottom: 14px;
}

.controls-row input[type='range'] {
  width: 100%;
}

.primary-button {
  border: none;
  background: linear-gradient(135deg, #38bdf8, #8b5cf6);
  color: white;
  border-radius: 10px;
  padding: 10px 18px;
  cursor: pointer;
  font-weight: 600;
}

.warning-banner {
  margin: 12px 0 18px;
  padding: 12px 14px;
  border-radius: 10px;
  background: rgba(245, 158, 11, 0.15);
  color: #fcd34d;
  border: 1px solid rgba(245, 158, 11, 0.3);
}

.checklist {
  margin: 0;
  padding-left: 18px;
  display: grid;
  gap: 10px;
}

.dag-tree {
  list-style: none;
  margin: 0;
  padding: 0;
  display: grid;
  gap: 10px;
  color: #dbeafe;
}

.split-head {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.section-space {
  margin-top: 18px;
}

@media (max-width: 980px) {
  .app-shell {
    flex-direction: column;
  }

  .sidebar {
    width: 100%;
    border-right: none;
    border-bottom: 1px solid rgba(148, 163, 184, 0.25);
  }

  .hero-grid, .tab-grid.two-col {
    grid-template-columns: 1fr;
  }
}
